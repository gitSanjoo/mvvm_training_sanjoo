pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "MVVM Training"
include ':app'


//settings.gradle consists of all modules that are active in our project
include ':mvvm_data'
include ':mvvm_domain'
include ':mvvm_presentation'
include ':mvvm_common'
